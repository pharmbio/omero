---
#continue: "true"
transfer: "ln_s"
checksum_algorithm: "File-Size-64"
#logprefix: "logs/"
#output: "yaml"
path: "P009041_name_path.tsv"
# Default columns for the regular screens.
# This may need to be modified in other bulk files.
columns:
    - name
    - path

